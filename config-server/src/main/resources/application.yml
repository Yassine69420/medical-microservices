server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Yassine69420/medical-config-repo
          default-label: main

management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info
